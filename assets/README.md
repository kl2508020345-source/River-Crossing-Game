# Assets Folder

This project ships with a full `assets/` structure (images/sounds) for an academic submission.

- The current build draws sprites procedurally (no external PNG required).
- Sound effects are generated procedurally at runtime (no external WAV required).

If you want to replace placeholders, drop your PNG/WAV files into:

- `assets/images/`
- `assets/sounds/`

